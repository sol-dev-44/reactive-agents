# @reactive-agent/react

## 0.1.0 — Initial release

- `useAgent` hook with streaming and abort support
- `useAgentResult` hook for cached result access
- `AgentApiProvider` context for dependency injection
