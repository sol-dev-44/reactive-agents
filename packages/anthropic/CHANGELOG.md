# @reactive-agent/anthropic

## 0.1.0 — Initial release

- Anthropic Claude provider adapter
- Streaming support via `messages.stream()`
- Tool call parsing from streaming events
- Error classification (rate limit, auth, server, connection)
- Cost estimation with model-specific pricing
